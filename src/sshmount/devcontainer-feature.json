{
    "name": "ssh-mount",
    "id": "sshmount",
    "version": "0.0.2",
    "description": "Mounts the host's ~/.ssh directory into the container",
    "options": {},
    "mounts": [
        {
            "type": "volume",
            "source": "${localEnv:HOME}/.ssh",
            "target": "/root/.ssh,readonly"
        },
        {
            "type": "volume",
            "source": "${localEnv:HOME}/.ssh",
            "target": "/home/vscode/.ssh,readonly"
        }
    ]
}
